<body>
    <div class="table-responsive">
        <table class="table v-middle" >
            <thead>
                <tr class="bg-light">
                    <th class="border-top-0">Images</th>
                    <th class="border-top-0">Name</th>
                    <th class="border-top-0">Price</th>
                    <th class="border-top-0">Total Amount</th>       
                    <th class="border-top-0">Total Price</th>                                   
                </tr>
            </thead>
                <tbody>
                    {{# each orderDetailsList}}

                    {{# each product}}
                    <tr>
                        <td> <image src="http://localhost:5000/public/photo/{{image}}"
                                    style="width: 50px; height: 50px"></image>
                        </td>
                        <td>{{name}}</td>
                        <td>{{price}}</td>
                        {{/each}}
                        <td>{{amount}}</td>
                        <td>{{price}}</td>
                    </tr>
                    {{/each}}
                </tbody>
        </table>
    </div>
</body>
